"""Optional LettuceDetect adapter."""

from __future__ import annotations


def predict(audit, thresholds):
    """Local-only placeholder."""
    return "Insufficient", 1, "Unknown", "LettuceDetect adapter is an optional local extension point."
