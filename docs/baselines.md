# Baselines

The baseline suite is designed around failure modes: surface overlap, semantic similarity, relation modeling without retrieval attribution, sufficiency without SC Gate, sufficient-context-only, learned interpretable features, local LettuceDetect, and REFIND-inspired context sensitivity.
